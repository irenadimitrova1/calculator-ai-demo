> *This was generated by AI during triage.*

## Triage outcome: wontfix (duplicate of planned v2 scope)

Thanks for the detailed report and screenshot. The `10 + 5 × 10 = 150` result is **correct for v1** — immediate left-to-right execution with the resolved `full-typed-history` expression trail (`pm-q2-chaining-trail`).

The two modes you described map to work already scoped in **[Calculator v2](docs/product/features/calculator-v2.md)**:

| Your proposal | v2 equivalent |
|---------------|---------------|
| Simple / aggregate (immediate execution, `15 ×` rewired trail) | **Basic mode** — immediate execution; trail stays `full-typed-history` per PM decision |
| History / PEMDAS (`10 + 5 × 10` → `60`) | **Scientific mode** — expression evaluated with PEMDAS on equals |

Adding a separate mode toggle or rewired-trail variant in v1 would duplicate v2 and reopen a resolved PM question. Decision recorded in [`.out-of-scope/dual-evaluation-modes.md`](.out-of-scope/dual-evaluation-modes.md).

Track PEMDAS and mode switching via the v2 feature doc and `/to-spec` → `/to-tickets` flow when v2 is ready to ship.
